# PeopleDesk

PeopleDesk is an employee management system with a Django API and a Next.js frontend.

## Projects

- `api/` - Django REST API
- `web/` - Next.js web application

## Development

Run the API and frontend from their respective directories. Each project keeps its own dependency and environment configuration.